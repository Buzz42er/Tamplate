import React from 'react'
import Logo from '../small-components/Logo';
import NavButton from '../small-components/NavButton';

const Header = () => {
  return (
    <header className="App-header">
        <Logo/>
        <NavButton/>
      </header>
  )
}

export default Header
